name: medicall
# force clean cache 1
description: MédiCall — Application de téléconsultation médicale
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  google_fonts: ^6.1.0
  provider: ^6.1.1
  shared_preferences: ^2.3.0
  intl: ^0.19.0

  # Firebase core
  firebase_core: ^2.24.2
  firebase_auth: ^4.16.0
  cloud_firestore: ^4.14.0
  firebase_storage: ^11.6.0
  firebase_messaging: ^14.7.10

  # Utilitaires
  image_picker: ^1.0.7
  flutter_local_notifications: ^17.0.0
  uuid: ^4.3.3
  cached_network_image: ^3.3.1
 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true


